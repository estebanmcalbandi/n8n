# Generamos todas las variables necesarias para n8n
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=esteb
N8N_BASIC_AUTH_PASSWORD=esteb1234
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http

# Configuramos el almacenamiento de datos en un volumen persistente
N8N_DATA_FOLDER=/home/node/.n8n
# N8N_DATA_FOLDER=/data/n8n_data
# VOLUMES:
#   - ./n8n_data:/home/node/.n8n
#   - /data/n8n_data:/home/node/.n8n




# Otras configuraciones opcionales
# N8N_LOG_LEVEL=info
# N8N_EDITOR_BASE_URL=http://localhost:5678/
# N8N_ALLOW_UNSAFE_OPERATIONS=true
# N8N_EMAIL_MODE=smtp
# N8N_SMTP_HOST=smtp.example.com
# N8N_SMTP_PORT=587
#
# Asegúrate de ajustar las variables según tus necesidades y entorno
# Guarda este archivo como .env en el mismo directorio que tu docker-compose.yml
# Luego, puedes iniciar n8n con: docker-compose --env-file .env up -d
# Recuerda crear el directorio para el volumen persistente
# mkdir -p ./n8n_data
# o
# mkdir -p /data/n8n_data
# dependiendo de la ruta que hayas elegido para N8N_DATA_FOLDER
# Finalmente, asegúrate de que los permisos del directorio sean correctos
# chown -R 1000:1000 ./n8n_data
# o
# chown -R 1000:1000 /data/n8n_data
# para que el contenedor n8n pueda acceder a él correctamente
# Ahora puedes acceder a n8n en http://localhost:5678 con las credenciales configuradas
# Usuario: esteb
# Contraseña: esteb1234
# ¡Disfruta automatizando con n8n!